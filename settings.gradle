rootProject.name = "BombusMod"

if (enableAndroid.toBoolean()) {
    include ":mtm", ":android"
}
if (enableIOS.toBoolean()) {
    include  ":ios"
}
if (enableDesktop.toBoolean()) {
    include  ":desktop"
}
